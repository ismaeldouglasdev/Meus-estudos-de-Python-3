"""
Você vai criar uma função que calcula a área de
duas formas geométricas: um retângulo e um círculo.
A função deve receber os parâmetros
necessários para cada forma e retornar a área correspondente.
"""
def area(m1,m2,m3):
    area_retangulo = m1 * m2
    area_circulo = 3.14 * (m3 ** 2)
return
